# 项目演示脚本

1. 使用 CMake 编译项目并运行全部测试。
2. 启动程序，选择 `Show map` 展示节点、边和权重。
3. 选择 Dijkstra，查询节点 1 到节点 6，说明访问顺序和最终路径。
4. 使用相同起终点运行 A*，对比访问节点数量。
5. 展示 `data/` 中的 CSV，说明地图数据可复现、可替换。
6. 总结当前终端版本成果，并介绍图形界面和动画的后续计划。

